/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'link.storjshare.io',
            'app.sandbox.midtrans.com'
        ],
    },
}

module.exports = nextConfig
